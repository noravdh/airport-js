'use strict';

describe('Airport', function(){
  var airport;
  var plane;
  beforeEach(function(){
    airport = new Airport();
    plane = 
  });

  it('can land at an airport', function(){
    expect(airport.planes).toEqual([]);
  });
});